{
  "name": "timesheet_app",
  "version": "0.0.1",
  "description": "A basic timesheet application",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install && npm run-script build && mv build ../timesheet_app"
  },
  "author": "Michael Hanley",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "nodemon": "^1.18.4",
    "pg": "^7.5.0",
    "pg-hstore": "^2.3.2",
    "sequelize": "^4.39.1"
  }
}
